#!/usr/bin/env bash
# Tiny wrapper: invoke secret.py under the project venv.
# Resolves its own location so it works from any cwd and from PATH.
set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$DIR/../.venv/bin/python" "$DIR/../secret.py" "$@"
